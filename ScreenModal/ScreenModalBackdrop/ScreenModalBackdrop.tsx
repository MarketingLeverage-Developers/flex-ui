import Modal from 'headless/Modal/Modal';
import styles from './ScreenModalBackdrop.module.scss';

const ScreenModalBackdrop = () => {
    return <Modal.Backdrop className={styles.ScreenModalBackdrop} />;
};

export default ScreenModalBackdrop;
