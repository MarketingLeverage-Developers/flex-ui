import Modal from 'headless/Modal/Modal';
import styles from './BasicModalBackdrop.module.scss';

const BasicModalBackdrop = () => {
    return <Modal.Backdrop className={styles.BasicModalBackdrop} />;
};

export default BasicModalBackdrop;
